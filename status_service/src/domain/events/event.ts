/**
 * Represents a base interface for all event objects in the system.
 * 
 * The `Event` interface is a marker for event classes that encapsulate
 * occurrences within the system. Each event represents an action or a change
 * in the system's state. These events can be dispatched, consumed, and handled
 * by event listeners or handlers.
 */
export interface Event {
}
