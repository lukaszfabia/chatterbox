/**
 * Represents a base interface for queries.
 * 
 * All query classes should implement this interface, marking them as queries that can be executed
 * in the system.
 */
export interface Query { }
